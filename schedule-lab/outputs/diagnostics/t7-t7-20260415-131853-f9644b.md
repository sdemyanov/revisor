---
probe: B
test_id: T7
task_name: lab-t7-relative-paths
---

# T7 Probe B (Relative Path) Diagnostic

- probe: B
- test_id: T7
- task_name: lab-t7-relative-paths
- local_timestamp: 2026-04-15 13:18:53 PDT (-0700)
- timezone: PDT (-0700)
- run_slug: t7-20260415-131853-f9644b
- cwd_before_relative_writes: /sessions/hopeful-lucid-goldberg
- planned_relative_paths:
  - ./outputs/path-probe/current-location.txt
  - ./outputs/path-probe/nested/check.txt
- resolved_absolute_paths_via_realpath:
  - /sessions/hopeful-lucid-goldberg/outputs/path-probe/current-location.txt
  - /sessions/hopeful-lucid-goldberg/outputs/path-probe/nested/check.txt

## Note on host-vs-sandbox path mapping

The task spec references host paths rooted at `/Users/sergey/Projects/revisor-plugin/`.
That path is not directly reachable from this sandbox. The user-selected folder is
mounted at `/sessions/hopeful-lucid-goldberg/mnt/revisor-plugin/` which corresponds
to the host folder `/Users/sergey/Projects/revisor-plugin/`. All "absolute" writes in
this run therefore go to the mount path, and relative writes resolve against the
shell cwd (`/sessions/hopeful-lucid-goldberg`), which is NOT inside the mounted
workspace — so relative writes will NOT land in the lab outputs tree, and will NOT
be visible to the user on the host.

## Outcome (STEP 2)

- relative_writes: succeeded
- per_path_status:
  - { path: "./outputs/path-probe/current-location.txt", success: true, exit_code: 0, error_text_if_any: null }
  - { path: "./outputs/path-probe/nested/check.txt", success: true, exit_code: 0, error_text_if_any: null }
  - { path: "mkdir -p ./outputs/path-probe", success: true, exit_code: 0, error_text_if_any: null }
  - { path: "mkdir -p ./outputs/path-probe/nested", success: true, exit_code: 0, error_text_if_any: null }
- actual_pwd_after_writes: /sessions/hopeful-lucid-goldberg

## Key finding

The relative writes succeeded AT THE SHELL CWD, which is `/sessions/hopeful-lucid-goldberg`
(the session scratchpad), NOT inside the user's mounted workspace at
`/sessions/hopeful-lucid-goldberg/mnt/revisor-plugin/`. Therefore the files are
NOT visible to the user on the host filesystem. They will be cleaned up when
the session is reclaimed. This confirms that scheduled tasks running in Cowork
resolve relative paths against the ephemeral session scratchpad, not against
the user-selected workspace folder — which is exactly the failure mode T7 was
designed to detect. Prompts that run on a schedule must use absolute paths
rooted at the mounted workspace (on host: `/Users/sergey/Projects/revisor-plugin/...`;
in-sandbox equivalent: `/sessions/hopeful-lucid-goldberg/mnt/revisor-plugin/...`).
