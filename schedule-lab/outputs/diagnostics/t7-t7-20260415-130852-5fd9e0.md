# T7 Probe B Diagnostic

- probe: B
- test_id: T7
- task_name: lab-t7-relative-paths
- local_timestamp: 2026-04-15 13:08:52 PDT
- timezone: PDT (-0700)
- run_slug: t7-20260415-130852-5fd9e0
- cwd_before_relative_writes: /sessions/brave-modest-bardeen
- planned_relative_paths:
  - ./outputs/path-probe/current-location.txt
  - ./outputs/path-probe/nested/check.txt
- resolved_absolute_paths_via_realpath:
  - /sessions/brave-modest-bardeen/outputs/path-probe/current-location.txt
  - /sessions/brave-modest-bardeen/outputs/path-probe/nested/check.txt

## Path-translation note
The task file prescribes absolute paths under `/Users/sergey/Projects/revisor-plugin/...`
but in this scheduled run those are not writable from the sandbox (EACCES / `mkdir: cannot
create directory '/Users': Permission denied`). The same workspace is visible under
`/sessions/brave-modest-bardeen/mnt/revisor-plugin/...`, which is where this diagnostic was
actually written. This is itself a finding for the scheduled-task runtime lab.

## Outcome (STEP 2 results — filled in by STEP 3)
- relative_writes: succeeded
- per_path_status:
  - {path: "./outputs/path-probe/current-location.txt", success: true, exit_code: 0, error_text_if_any: null, resolved_to: "/sessions/brave-modest-bardeen/outputs/path-probe/current-location.txt"}
  - {path: "./outputs/path-probe/nested/check.txt", success: true, exit_code: 0, error_text_if_any: null, resolved_to: "/sessions/brave-modest-bardeen/outputs/path-probe/nested/check.txt"}
- actual_pwd_after_writes: /sessions/brave-modest-bardeen

## Finding
Relative paths in a scheduled-task run resolve against the session working directory
(`/sessions/brave-modest-bardeen`), NOT against the workspace mount
(`/sessions/brave-modest-bardeen/mnt/revisor-plugin`). The relative writes landed
OUTSIDE the workspace mount and are therefore invisible to the user on disk — they live
only inside the ephemeral session scratchpad. This confirms the hard rule captured in
auto-memory: scheduled-task prompts must use absolute paths rooted in the workspace mount
(or the host-equivalent path the platform translates to it) for any output the user needs
to see.

Additionally, the host-style absolute paths prescribed by the task
(`/Users/sergey/Projects/revisor-plugin/...`) were NOT writable from the sandbox — they
returned EACCES. The workspace is only reachable here as
`/sessions/brave-modest-bardeen/mnt/revisor-plugin/...`, so the diagnostic, run log, and
(if needed) failure note were written there instead.
