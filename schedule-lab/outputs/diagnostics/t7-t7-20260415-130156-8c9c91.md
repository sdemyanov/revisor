# T7 Probe B Diagnostic (pre-relative-write)

- probe: B
- test_id: T7
- task_name: lab-t7-relative-paths
- local_timestamp: 2026-04-15 13:01:56 PDT
- timezone: PDT (America/Los_Angeles)
- run_slug: t7-20260415-130156-8c9c91
- cwd_before_relative_writes: /sessions/confident-wizardly-bohr
- planned_relative_paths:
  - ./outputs/path-probe/current-location.txt
  - ./outputs/path-probe/nested/check.txt
- resolved_absolute_paths_via_realpath:
  - /sessions/confident-wizardly-bohr/outputs/path-probe/current-location.txt
  - /sessions/confident-wizardly-bohr/outputs/path-probe/nested/check.txt

## Notes
- The task prompt specified the absolute host path `/Users/sergey/Projects/revisor-plugin/schedule-lab/outputs/...`. That host path is not visible inside the scheduled-task sandbox; the same directory is mounted at `/sessions/confident-wizardly-bohr/mnt/revisor-plugin/schedule-lab/outputs/`. The diagnostic was written to the mounted equivalent so that it lands in the same on-disk location the host sees.
- The scheduled task's cwd is `/sessions/confident-wizardly-bohr` (the scratchpad), NOT the mounted project folder. So `./outputs/...` relative writes resolve into the scratchpad, not into the schedule-lab outputs directory.

## Outcome (post STEP 2)

- relative_writes: succeeded
- per_path_status:
  - {path: "./outputs/path-probe/current-location.txt", success: true, exit_code: 0, error_text_if_any: null, final_abs_path: "/sessions/confident-wizardly-bohr/outputs/path-probe/current-location.txt"}
  - {path: "./outputs/path-probe/nested/check.txt", success: true, exit_code: 0, error_text_if_any: null, final_abs_path: "/sessions/confident-wizardly-bohr/outputs/path-probe/nested/check.txt"}
- actual_pwd_after_writes: /sessions/confident-wizardly-bohr
- key_finding: Relative writes resolved into the sandbox scratchpad (`/sessions/confident-wizardly-bohr/outputs/...`), NOT into the mounted project folder's `schedule-lab/outputs/path-probe` tree. The scheduled task runs with cwd = the session scratchpad, so any `./outputs/...` writes are ephemeral and invisible to the user on the host.
